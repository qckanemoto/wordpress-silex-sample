<h2>著者：{% do the_author() %}</h2>
{% for post in get_posts() %}
    {% if have_posts() %}
        {{ the_post() }}
        <h3>{{ the_title() }}</h3>
        <p>{{ the_content() }}</p>
    {% endif %}
{% endfor %}
